# Installs this project into the venv, so `uvicorn fppvote.service:app` works
# from any directory without PYTHONPATH. Runtime dependencies are pinned in
# pyproject.toml — one list, so the two cannot drift.
-e .

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.2
